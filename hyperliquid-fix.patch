--- a/src/types.rs
+++ b/src/types.rs
@@ -174,7 +174,8 @@
             pub name: String,
             pub sz_decimals: u64,
             pub max_leverage: u64,
+            #[serde(default)]
             pub only_isolated: bool,
         }
